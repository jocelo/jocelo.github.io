import { LightDividerDirective } from './light-divider.directive';

describe('LightDividerDirective', () => {
  it('should create an instance', () => {
    const directive = new LightDividerDirective();
    expect(directive).toBeTruthy();
  });
});
